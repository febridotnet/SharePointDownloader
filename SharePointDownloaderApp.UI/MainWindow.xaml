<Window x:Class="SharePointDownloaderApp.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SharePointDownloaderApp.UI"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header Information -->
        <StackPanel Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="SharePoint Automated Downloader" FontSize="18" FontWeight="Bold"/>
            <!-- Tambahkan x:Name="TxtJadwal" di bawah ini -->
            <TextBlock x:Name="TxtJadwal" Text="Load schedule..." Foreground="Gray" Margin="0,5,0,0"/>
        </StackPanel>

        <!-- Control Buttons -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,15">
            <Button x:Name="BtnDownloadNow" Content="Download Now" Width="140" Height="32" Click="BtnDownloadNow_Click" Margin="0,0,10,0"/>
            <TextBlock x:Name="TxtStatus" Text="Scheduler Status: Active" VerticalAlignment="Center" FontWeight="SemiBold" Foreground="Green"/>
        </StackPanel>

        <!-- Activity Log -->
        <GroupBox Grid.Row="2" Header="Activity Log">
            <TextBox x:Name="TxtLog" IsReadOnly="True" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" FontFamily="Consolas" FontSize="12" Background="#F9F9F9"/>
        </GroupBox>
    </Grid>
</Window>
